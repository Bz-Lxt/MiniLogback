# MiniLogback 评测说明

本项目是基于Go语言的高性能全异步分布式日志流缓冲收集中间件（Log Appender），旨在解决高性能全异步分布式日志流缓冲收集中间件（Log Appender）相关的工程问题，使用了Go、React，功能有Ring Buffer 水位大屏、字节流内存泄漏审计中心、基于无锁环形队列的多生产者单消费者（MPSC）日、批量零拷贝流式字节刷盘引擎。

Go 模块位于 仓库根目录。评测入口：在该目录执行 `go test ./...`。
